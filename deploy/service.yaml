apiVersion: v1
kind: Service
metadata:
  name: hairpin-server
  namespace: default
spec:
  type: NodePort
  selector:
    app: hairpin-server
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30300
